import React from "react";
import ReactDOM from "react-dom";
import Demo from "./src/xflow";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <Demo />
  </React.StrictMode>
);
